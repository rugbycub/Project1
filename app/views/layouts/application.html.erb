<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=.8">
    <title><%= content_for?(:title) ? yield(:title) : "Barcard" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Barcard" %>">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="container">
      <header>
        <%= render 'layouts/navigation' %>
      </header>
    </div>
   
    <div class="container">
      <main role="main">
      <%= render 'layouts/messages' %>
      <%= yield %>
    </main>


    </div>
  </body>
</html>
